<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Заливка -->
    <solid android:color="@color/background_dark"/>

    <!-- Скругленные углы -->
    <corners android:radius="20dp"/>

    <!-- Граница -->
    <stroke
        android:width="1dp"
        android:color="@color/primary_light_teal"/>

    <!-- Внутренние отступы -->
    <padding
        android:left="4dp"
        android:top="4dp"
        android:right="4dp"
        android:bottom="4dp"/>
</shape>